import 'package:flutter/material.dart';

void main() => runApp(const GoodReaderApp());

class GoodReaderApp extends StatelessWidget {
  const GoodReaderApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
